Chat project – Fullstack Chat Application

Chat project is a fullstack web application for online chatting that allows users to register, log in, and exchange messages in real-time.
This project demonstrates a complete Fullstack setup: Frontend, Backend, and Database integration.

🛠 Technologies Used

Frontend:

React

React Router

Backend:

Node.js

Express.js

Database:

MySQL

🚀 How to Run Locally
1️⃣ Clone the repository
git clone https://github.com/username/chatway.git

2️⃣ Configure environment variables

Create a .env file inside the server folder and add your MySQL credentials:

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=chatway
PORT=5000

3️⃣ Run Backend
cd server
npm install
npm run dev

4️⃣ Run Frontend

In a separate terminal:

cd client
npm install
npm run dev
